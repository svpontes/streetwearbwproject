@page "/"
@using StreetTshirtApp.Models

<PageTitle>Street T-Shirt - Official Store</PageTitle>

<div class="position-relative overflow-hidden p-0 m-0 text-center bg-dark w-100">
    <img src="/images/banner-principal.png" alt="Street T-Shirt Collection Banner" class="w-100 img-fluid" style="height: 600px; object-fit: cover;" />
    <div class="col-md-5 p-lg-5 mx-auto my-5 position-absolute top-50 start-50 translate-middle text-white" style="background-color: rgba(0,0,0,0.5); border-radius: 10px;">
        <h1 class="display-4 fw-bold">Urban Exclusive</h1>
        <p class="lead fw-normal">Check out our new collection of exclusive HD prints.</p>
        <a class="btn btn-ligth btn-lg rounded-2 fw-bold custom-bw-btn" href="#catalog">Shop the Collection</a>
    </div>
</div>

<div id="catalog" class="container mt-5 mb-5">
    <h2 class="mb-4 fw-bold text-center">Featured Designs</h2>

    <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 row-cols-xl-4 g-4">
        @foreach (var product in featuredProducts)
        {
            <div class="col">
                <div class="card h-100 shadow-sm border-0.5" style="background-color: #f8f9fa;">
                    
                    <a href="/product/@product.Id" class="text-decoration-none">
                        <div class="p-4 text-center">
                            <img src="@product.ImageUrl" alt="@product.Name" class="img-fluid" style="max-height: 220px; object-fit: contain;" />
                        </div>
                    </a>
                    
                    <div class="card-body d-flex flex-column text-dark">
                        
                        <a href="/product/@product.Id" class="text-decoration-none text-dark">
                            <h5 class="card-title fw-bold">@product.Name</h5>
                        </a>
                        
                        <p class="card-text small text-muted flex-grow-1">@product.Description</p>
                        
                        <div class="d-flex justify-content-between align-items-center mt-auto">
                            <span class="fs-5 fw-bold" style="color: #747474;">$@product.Price.ToString("0.00")</span>
                            <a href="/product/@product.Id" class="btn btn-dark btn-sm fw-bold">Add</a>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>

@code {
    private List<Product> featuredProducts = new();

    protected override void OnInitialized()
    {
        // Carrega todos os produtos reais do nosso novo MockData
        featuredProducts = MockData.GetMockProducts();
    }
}